DAG for __ distro for {{ epoch }} with changed packages and their reverse dependencies highlighted

```mermaid
{{ core_mermaid }}
```

List of changed packages and their immediate reverse dependencies
{% for pkg, deps in filtered_dict.items() %}
Package:
    {{ pkg }}
Reverse Dependencies:
    {{ deps }}
{% endfor %}
