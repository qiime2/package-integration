<h1>Distro Trial Report</h1>

<h3>List of changed packages and their immediate reverse dependencies</h3>
<table>
    <tr><th>Changed Package</th><th>Reverse Dependencies</th></tr>
    {%- for pkg, deps in filtered_items -%}
    <tr>
        <td> {{ pkg }} </td>
        <td>
            <ul>
            {%- for dep in sorted(deps) -%}
                <li>{{ dep }}</li>
            {%- endfor -%}
            </ul>
        </td>
    </tr>
    {%- endfor -%}
</table>

<h3>List of all packages that require a re-test due to the changes above</h3>
    <ul>
    {%- for pkg in sorted(pkgs_to_test) -%}
        <li>{{ pkg }}</li>
    {%- endfor -%}
    </ul>

<h3>DAG for __ distro for {{ epoch }} with changed packages and their reverse dependencies highlighted</h3>

```mermaid
{{ core_mermaid }}
```
